// The House Lights type system (DESIGN.md): Montserrat for display/headers,
// Open Sans for body. Registered via next/font in app/layout.tsx; these
// stacks are safe anywhere the --font-* variables are in scope.
export const DISPLAY = 'var(--font-montserrat), system-ui, arial, sans-serif';
export const BODY = 'var(--font-open-sans), system-ui, arial, sans-serif';
